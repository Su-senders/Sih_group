@model IEnumerable<Sih.Entities.Gestion.AvionEntity>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="AddOrUpdate">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Capacite)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumAvion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Capacite)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumAvion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AvionEntityId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AvionEntityId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AvionEntityId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
