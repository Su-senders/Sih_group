@model IEnumerable<Sih.Entities.Localisation.DepartureTownEntity>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="AddOrUpdate">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UsagerEntityId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HadjEntityId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VilleDepart)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UsagerEntityId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HadjEntityId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VilleDepart.VilleEntityId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DepartureTownEntityId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DepartureTownEntityId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DepartureTownEntityId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
